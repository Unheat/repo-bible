# SYSTEM ROLE & DIRECTIVE
You are a Staff Software Architect. We are migrating an existing, fully functional production-grade AI documentation application (currently tightly coupled to a proprietary Serverless platform) into a standard, open-source, self-hostable Full-Stack application (Vite/React + Node.js/Express + SQLite).

**CRITICAL RULE:** DO NOT rewrite the application from scratch. The existing logic for AST chunking, Markdown rendering, Mermaid graph generation, and LLM prompting is highly optimized and MUST be preserved. Your job is to **refactor, decouple, and extend**.

---

# CONTEXT: THE "CODEBASE BIBLE" APP
The application ingests GitHub repositories, clones the file tree, embeds the source using OpenRouter, and runs two AI agents (a Mapper for architecture and a Deep-Dive for per-file docs). 

Currently, the codebase is a flat structure outputted by the proprietary platform:
- Frontend: `dist/interfaces/web/`
- Backend Logic: `dist/methods/src/`
- Proprietary DB schemas: `dist/methods/src/tables/`

Our ultimate goal is to make this an open-source product that anyone can clone, configure via `.env` or UI, and run locally.

---

# EXECUTION PLAN

Acknowledge this prompt by confirming you understand the constraints. We will execute this plan step-by-step. Do not rush to Phase 2 until Phase 1 is complete and approved.

## Phase 1: Decoupling & Infrastructure Restructure
1. **Directory Setup:** Reorganize the code into a standard monorepo:
   - `/frontend` (Vite + React)
   - `/backend` (Node.js + Express)
2. **Remove Proprietary SDKs:** Strip all `@mindstudio-ai/agent` and `@mindstudio-ai/interface` imports. 
3. **Environment Variables:** Create a `.env.example` and `.env` setup in the backend for `OPENROUTER_API_KEY`, `OPENAI_API_KEY` (for embeddings), and `GITHUB_TOKEN`.
4. **Backend API Wrapper:** Wrap the existing logic in `dist/methods/` with standard Express.js REST endpoints (e.g., `POST /api/ingest`, `POST /api/generate`).

## Phase 2: Database Migration
1. **SQLite Integration:** Replace the proprietary `db.defineTable` logic in the `tables/` directory with a standard SQLite implementation (using Prisma ORM or raw `sqlite3`). 
2. Maintain the exact same schema shapes for `repositories`, `files`, `code_chunks`, and `generated_docs`.

## Phase 3: The V2 UI (Hierarchical Sidebar)
1. In the frontend (`RepoPage.tsx` and related components), the current file list is flat.
2. Refactor the Sidebar to use a **Hierarchical Folder Tree View**. 
3. Logic: Parse the `filePath` strings from the database into nested folders that users can toggle (expand/collapse), mirroring a real IDE like VS Code.

## Phase 4: BYOK (Bring Your Own Key) Settings UI
1. To make this friendly for non-tech users who clone the repo, build a "Settings" modal in the frontend UI.
2. Allow users to input their `OpenRouter API Key` and `GitHub Token` directly into the UI.
3. Save these keys securely (e.g., to `localStorage` encrypted, or passed to a backend endpoint that writes to a local `config.json` file) so the app can use them dynamically instead of relying purely on `.env`.

## Phase 5: Sharing & Export
1. Implement a feature to export the generated "Bible".
2. **Action:** Create a "Download HTML" feature that bundles the generated Markdown, Mermaid diagrams, and CSS into a single, offline-viewable `.html` file that users can share with their team.

---

**Initial Action Required from you:**
Analyze the provided codebase. Output the new exact folder structure we will use for Phase 1. Wait for my confirmation before writing the code to move the files.